import React from "react";

const SearchBar = () => {
  return (
    <input
      type="search"
      className="form-control ds-input"
      id="search-input"
      placeholder="Search..."
    />
  );
};

export default SearchBar;
